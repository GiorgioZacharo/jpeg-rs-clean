// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// ==============================================================

#include <systemc>
#include <iostream>
#include <cstdlib>
#include <cstddef>
#include <stdint.h>
#include "SysCFileHandler.h"
#include "ap_int.h"
#include "ap_fixed.h"
#include <complex>
#include <stdbool.h>
#include "autopilot_cbe.h"
#include "ap_stream.h"
#include "hls_stream.h"
#include "hls_half.h"
#include "hls_signal_handler.h"

using namespace std;
using namespace sc_core;
using namespace sc_dt;


// [dump_struct_tree [build_nameSpaceTree] dumpedStructList] ---------->


// [dump_enumeration [get_enumeration_list]] ---------->


// wrapc file define: "BUS_SRC"
#define AUTOTB_TVIN_BUS_SRC  "../tv/cdatafile/c.IZigzagMatrix_f2r_forBody_s2e_forEnd.autotvin_BUS_SRC.dat"
// wrapc file define: "BUS_DST"
#define AUTOTB_TVOUT_BUS_DST  "../tv/cdatafile/c.IZigzagMatrix_f2r_forBody_s2e_forEnd.autotvout_BUS_DST.dat"
#define AUTOTB_TVIN_BUS_DST  "../tv/cdatafile/c.IZigzagMatrix_f2r_forBody_s2e_forEnd.autotvin_BUS_DST.dat"
// wrapc file define: "imatrix"
#define AUTOTB_TVIN_imatrix  "../tv/cdatafile/c.IZigzagMatrix_f2r_forBody_s2e_forEnd.autotvin_imatrix.dat"
// wrapc file define: "omatrix"
#define AUTOTB_TVIN_omatrix  "../tv/cdatafile/c.IZigzagMatrix_f2r_forBody_s2e_forEnd.autotvin_omatrix.dat"

#define INTER_TCL  "../tv/cdatafile/ref.tcl"

// tvout file define: "BUS_DST"
#define AUTOTB_TVOUT_PC_BUS_DST  "../tv/rtldatafile/rtl.IZigzagMatrix_f2r_forBody_s2e_forEnd.autotvout_BUS_DST.dat"

class INTER_TCL_FILE {
	public:
		INTER_TCL_FILE(const char* name) {
			mName = name;
			BUS_SRC_depth = 0;
			BUS_DST_depth = 0;
			imatrix_depth = 0;
			omatrix_depth = 0;
			trans_num =0;
		}

		~INTER_TCL_FILE() {
			mFile.open(mName);
			if (!mFile.good()) {
				cout << "Failed to open file ref.tcl" << endl;
				exit (1);
			}
			string total_list = get_depth_list();
			mFile << "set depth_list {\n";
			mFile << total_list;
			mFile << "}\n";
			mFile << "set trans_num "<<trans_num<<endl;
			mFile.close();
		}

		string get_depth_list () {
			stringstream total_list;
			total_list << "{BUS_SRC " << BUS_SRC_depth << "}\n";
			total_list << "{BUS_DST " << BUS_DST_depth << "}\n";
			total_list << "{imatrix " << imatrix_depth << "}\n";
			total_list << "{omatrix " << omatrix_depth << "}\n";
			return total_list.str();
		}

		void set_num (int num , int* class_num) {
			(*class_num) = (*class_num) > num ? (*class_num) : num;
		}
	public:
		int BUS_SRC_depth;
		int BUS_DST_depth;
		int imatrix_depth;
		int omatrix_depth;
		int trans_num;

	private:
		ofstream mFile;
		const char* mName;
};

extern "C" void IZigzagMatrix_f2r_forBody_s2e_forEnd (
int imatrix[64],
int omatrix[64]);

extern "C" void AESL_WRAP_IZigzagMatrix_f2r_forBody_s2e_forEnd (
int imatrix[64],
int omatrix[64])
{
	refine_signal_handler();
	fstream wrapc_switch_file_token;
	wrapc_switch_file_token.open(".hls_cosim_wrapc_switch.log");
	int AESL_i;
	if (wrapc_switch_file_token.good())
	{
		CodeState = ENTER_WRAPC_PC;
		static unsigned AESL_transaction_pc = 0;
		string AESL_token;
		string AESL_num;
		static AESL_FILE_HANDLER aesl_fh;


		// output port post check: "BUS_DST"
		aesl_fh.read(AUTOTB_TVOUT_PC_BUS_DST, AESL_token); // [[transaction]]
		if (AESL_token != "[[transaction]]")
		{
			exit(1);
		}
		aesl_fh.read(AUTOTB_TVOUT_PC_BUS_DST, AESL_num); // transaction number

		if (atoi(AESL_num.c_str()) == AESL_transaction_pc)
		{
			aesl_fh.read(AUTOTB_TVOUT_PC_BUS_DST, AESL_token); // data

			sc_bv<32> *BUS_DST_pc_buffer = new sc_bv<32>[64];
			int i = 0;

			while (AESL_token != "[[/transaction]]")
			{
				bool no_x = false;
				bool err = false;

				// search and replace 'X' with "0" from the 1st char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('X');
					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'BUS_DST', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				no_x = false;

				// search and replace 'x' with "0" from the 3rd char of token
				while (!no_x)
				{
					size_t x_found = AESL_token.find('x', 2);

					if (x_found != string::npos)
					{
						if (!err)
						{
							cerr << "WARNING: [SIM 212-201] RTL produces unknown value 'X' on port 'BUS_DST', possible cause: There are uninitialized variables in the C design." << endl;
							err = true;
						}
						AESL_token.replace(x_found, 1, "0");
					}
					else
					{
						no_x = true;
					}
				}

				// push token into output port buffer
				if (AESL_token != "")
				{
					BUS_DST_pc_buffer[i] = AESL_token.c_str();
					i++;
				}

				aesl_fh.read(AUTOTB_TVOUT_PC_BUS_DST, AESL_token); // data or [[/transaction]]

				if (AESL_token == "[[[/runtime]]]" || aesl_fh.eof(AUTOTB_TVOUT_PC_BUS_DST))
				{
					exit(1);
				}
			}

			// ***********************************
			if (i > 0)
			{
				// RTL Name: BUS_DST
				{
					// bitslice(31, 0)
					// {
						// celement: omatrix(31, 0)
						// {
							sc_lv<32>* omatrix_lv0_0_63_1 = new sc_lv<32>[64];
						// }
					// }

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: omatrix(31, 0)
						{
							// carray: (0) => (63) @ (1)
							for (int i_0 = 0; i_0 <= 63; i_0 += 1)
							{
								if (&(omatrix[0]) != NULL) // check the null address if the c port is array or others
								{
									omatrix_lv0_0_63_1[hls_map_index].range(31, 0) = sc_bv<32>(BUS_DST_pc_buffer[hls_map_index].range(31, 0));
									hls_map_index++;
								}
							}
						}
					}

					// bitslice(31, 0)
					{
						int hls_map_index = 0;
						// celement: omatrix(31, 0)
						{
							// carray: (0) => (63) @ (1)
							for (int i_0 = 0; i_0 <= 63; i_0 += 1)
							{
								// sub                    : i_0
								// ori_name               : omatrix[i_0]
								// sub_1st_elem           : 0
								// ori_name_1st_elem      : omatrix[0]
								// output_left_conversion : omatrix[i_0]
								// output_type_conversion : (omatrix_lv0_0_63_1[hls_map_index]).to_uint64()
								if (&(omatrix[0]) != NULL) // check the null address if the c port is array or others
								{
									omatrix[i_0] = (omatrix_lv0_0_63_1[hls_map_index]).to_uint64();
									hls_map_index++;
								}
							}
						}
					}
				}
			}

			// release memory allocation
			delete [] BUS_DST_pc_buffer;
		}

		AESL_transaction_pc++;
	}
	else
	{
		CodeState = ENTER_WRAPC;
		static unsigned AESL_transaction;

		static AESL_FILE_HANDLER aesl_fh;

		// "BUS_SRC"
		char* tvin_BUS_SRC = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_BUS_SRC);

		// "BUS_DST"
		char* tvin_BUS_DST = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_BUS_DST);
		char* tvout_BUS_DST = new char[50];
		aesl_fh.touch(AUTOTB_TVOUT_BUS_DST);

		// "imatrix"
		char* tvin_imatrix = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_imatrix);

		// "omatrix"
		char* tvin_omatrix = new char[50];
		aesl_fh.touch(AUTOTB_TVIN_omatrix);

		CodeState = DUMP_INPUTS;
		static INTER_TCL_FILE tcl_file(INTER_TCL);
		int leading_zero;

		// [[transaction]]
		sprintf(tvin_BUS_SRC, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_BUS_SRC, tvin_BUS_SRC);

		sc_bv<32>* BUS_SRC_tvin_wrapc_buffer = new sc_bv<32>[64];

		// RTL Name: BUS_SRC
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: imatrix(31, 0)
				{
					// carray: (0) => (63) @ (1)
					for (int i_0 = 0; i_0 <= 63; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : imatrix[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : imatrix[0]
						// regulate_c_name       : imatrix
						// input_type_conversion : imatrix[i_0]
						if (&(imatrix[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> imatrix_tmp_mem;
							imatrix_tmp_mem = imatrix[i_0];
							BUS_SRC_tvin_wrapc_buffer[hls_map_index].range(31, 0) = imatrix_tmp_mem.range(31, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 64; i++)
		{
			sprintf(tvin_BUS_SRC, "%s\n", (BUS_SRC_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_BUS_SRC, tvin_BUS_SRC);
		}

		tcl_file.set_num(64, &tcl_file.BUS_SRC_depth);
		sprintf(tvin_BUS_SRC, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_BUS_SRC, tvin_BUS_SRC);

		// release memory allocation
		delete [] BUS_SRC_tvin_wrapc_buffer;

		// [[transaction]]
		sprintf(tvin_BUS_DST, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_BUS_DST, tvin_BUS_DST);

		sc_bv<32>* BUS_DST_tvin_wrapc_buffer = new sc_bv<32>[64];

		// RTL Name: BUS_DST
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: omatrix(31, 0)
				{
					// carray: (0) => (63) @ (1)
					for (int i_0 = 0; i_0 <= 63; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : omatrix[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : omatrix[0]
						// regulate_c_name       : omatrix
						// input_type_conversion : omatrix[i_0]
						if (&(omatrix[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> omatrix_tmp_mem;
							omatrix_tmp_mem = omatrix[i_0];
							BUS_DST_tvin_wrapc_buffer[hls_map_index].range(31, 0) = omatrix_tmp_mem.range(31, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 64; i++)
		{
			sprintf(tvin_BUS_DST, "%s\n", (BUS_DST_tvin_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_BUS_DST, tvin_BUS_DST);
		}

		tcl_file.set_num(64, &tcl_file.BUS_DST_depth);
		sprintf(tvin_BUS_DST, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_BUS_DST, tvin_BUS_DST);

		// release memory allocation
		delete [] BUS_DST_tvin_wrapc_buffer;

		// [[transaction]]
		sprintf(tvin_imatrix, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_imatrix, tvin_imatrix);

		sc_bv<32> imatrix_tvin_wrapc_buffer;

		// RTL Name: imatrix
		{
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_imatrix, "%s\n", (imatrix_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_imatrix, tvin_imatrix);
		}

		tcl_file.set_num(1, &tcl_file.imatrix_depth);
		sprintf(tvin_imatrix, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_imatrix, tvin_imatrix);

		// [[transaction]]
		sprintf(tvin_omatrix, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVIN_omatrix, tvin_omatrix);

		sc_bv<32> omatrix_tvin_wrapc_buffer;

		// RTL Name: omatrix
		{
		}

		// dump tv to file
		for (int i = 0; i < 1; i++)
		{
			sprintf(tvin_omatrix, "%s\n", (omatrix_tvin_wrapc_buffer).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVIN_omatrix, tvin_omatrix);
		}

		tcl_file.set_num(1, &tcl_file.omatrix_depth);
		sprintf(tvin_omatrix, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVIN_omatrix, tvin_omatrix);

// [call_c_dut] ---------->

		CodeState = CALL_C_DUT;
		IZigzagMatrix_f2r_forBody_s2e_forEnd(imatrix, omatrix);

		CodeState = DUMP_OUTPUTS;

		// [[transaction]]
		sprintf(tvout_BUS_DST, "[[transaction]] %d\n", AESL_transaction);
		aesl_fh.write(AUTOTB_TVOUT_BUS_DST, tvout_BUS_DST);

		sc_bv<32>* BUS_DST_tvout_wrapc_buffer = new sc_bv<32>[64];

		// RTL Name: BUS_DST
		{
			// bitslice(31, 0)
			{
				int hls_map_index = 0;
				// celement: omatrix(31, 0)
				{
					// carray: (0) => (63) @ (1)
					for (int i_0 = 0; i_0 <= 63; i_0 += 1)
					{
						// sub                   : i_0
						// ori_name              : omatrix[i_0]
						// sub_1st_elem          : 0
						// ori_name_1st_elem     : omatrix[0]
						// regulate_c_name       : omatrix
						// input_type_conversion : omatrix[i_0]
						if (&(omatrix[0]) != NULL) // check the null address if the c port is array or others
						{
							sc_lv<32> omatrix_tmp_mem;
							omatrix_tmp_mem = omatrix[i_0];
							BUS_DST_tvout_wrapc_buffer[hls_map_index].range(31, 0) = omatrix_tmp_mem.range(31, 0);
                                 	       hls_map_index++;
						}
					}
				}
			}
		}

		// dump tv to file
		for (int i = 0; i < 64; i++)
		{
			sprintf(tvout_BUS_DST, "%s\n", (BUS_DST_tvout_wrapc_buffer[i]).to_string(SC_HEX).c_str());
			aesl_fh.write(AUTOTB_TVOUT_BUS_DST, tvout_BUS_DST);
		}

		tcl_file.set_num(64, &tcl_file.BUS_DST_depth);
		sprintf(tvout_BUS_DST, "[[/transaction]] \n");
		aesl_fh.write(AUTOTB_TVOUT_BUS_DST, tvout_BUS_DST);

		// release memory allocation
		delete [] BUS_DST_tvout_wrapc_buffer;

		CodeState = DELETE_CHAR_BUFFERS;
		// release memory allocation: "BUS_SRC"
		delete [] tvin_BUS_SRC;
		// release memory allocation: "BUS_DST"
		delete [] tvout_BUS_DST;
		delete [] tvin_BUS_DST;
		// release memory allocation: "imatrix"
		delete [] tvin_imatrix;
		// release memory allocation: "omatrix"
		delete [] tvin_omatrix;

		AESL_transaction++;

		tcl_file.set_num(AESL_transaction , &tcl_file.trans_num);
	}
}

