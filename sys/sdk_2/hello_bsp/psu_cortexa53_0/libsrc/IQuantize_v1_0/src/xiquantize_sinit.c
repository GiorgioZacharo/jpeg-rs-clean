// ==============================================================
// File generated by Vivado(TM) HLS - High-Level Synthesis from C, C++ and SystemC
// Version: 2017.4
// Copyright (C) 1986-2017 Xilinx, Inc. All Rights Reserved.
// 
// ==============================================================

#ifndef __linux__

#include "xstatus.h"
#include "xparameters.h"
#include "xiquantize.h"

extern XIquantize_Config XIquantize_ConfigTable[];

XIquantize_Config *XIquantize_LookupConfig(u16 DeviceId) {
	XIquantize_Config *ConfigPtr = NULL;

	int Index;

	for (Index = 0; Index < XPAR_XIQUANTIZE_NUM_INSTANCES; Index++) {
		if (XIquantize_ConfigTable[Index].DeviceId == DeviceId) {
			ConfigPtr = &XIquantize_ConfigTable[Index];
			break;
		}
	}

	return ConfigPtr;
}

int XIquantize_Initialize(XIquantize *InstancePtr, u16 DeviceId) {
	XIquantize_Config *ConfigPtr;

	Xil_AssertNonvoid(InstancePtr != NULL);

	ConfigPtr = XIquantize_LookupConfig(DeviceId);
	if (ConfigPtr == NULL) {
		InstancePtr->IsReady = 0;
		return (XST_DEVICE_NOT_FOUND);
	}

	return XIquantize_CfgInitialize(InstancePtr, ConfigPtr);
}

#endif

